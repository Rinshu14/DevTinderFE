const ignored = "ignored"
const intrested = "interested"
const reject = "rejected"
const accept = "accepted"

enum apiGenderEnum {
    male = "male",
    female = "female",
    others = "others"
}

const APIConstants = { ignored, intrested, reject, accept, apiGenderEnum }
 export default APIConstants;


